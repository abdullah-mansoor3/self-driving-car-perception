# Core runtime
numpy==1.26.4
opencv-python==4.10.0.84
onnx==1.17.0
onnxruntime==1.25.1
tqdm==4.66.5

# Model download helper
huggingface_hub==0.33.5

# TTS (optional but used by demo unless --no-tts)
kokoro==0.9.4
sounddevice==0.5.1
espeakng-loader==0.2.4

# Notes:
# 1) This file is CPU-safe by default and does not force a torch reinstall.
# 2) For GPU ONNX Runtime, install separately after NVIDIA driver is healthy:
#      pip uninstall -y onnxruntime
#      pip install onnxruntime-gpu==1.25.1
# 3) If you need to export depth ONNX or train locally, install a driver-compatible torch build separately.
